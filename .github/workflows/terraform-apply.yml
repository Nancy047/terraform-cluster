name: Terraform

on:
  push:
    branches:
      - main

jobs:
  terraform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Print GCP_SERVICE_ACCOUNT_KEY
        run: echo "${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}"

      - name: Create keys.json file
        run: echo "${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}" > keys.json

      - name: Print keys.json file
        run: cat keys.json

      # Add additional debugging steps here

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.0.11

      - name: Initialize Terraform
        run: terraform init

      - name: Validate Terraform
        run: terraform validate


      - name: Plan Terraform
        run: terraform plan

      - name: Apply Terraform
        if: github.ref == 'refs/heads/main'
        run: terraform apply -auto-approve
